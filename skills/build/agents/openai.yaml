interface:
  display_name: "Fiction Build"
  short_description: "Build EPUBs from fiction projects"
  default_prompt: "Use $build to build an EPUB from this fiction project."
policy:
  allow_implicit_invocation: false
